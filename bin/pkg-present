#!/bin/bash
# Returns true (exit 0) if ALL packages are installed
# No romarchy- prefix per naming convention

for pkg in "$@"; do
  if ! pacman -Q "$pkg" &>/dev/null && ! (command -v yay &>/dev/null && yay -Q "$pkg" &>/dev/null); then
    exit 1
  fi
done
exit 0
