#!/bin/bash

CURRENT_THEME_NAME=$(cat "$HOME/.local/share/romarchy/.current-theme")
THEME_USER_BACKGROUNDS="$HOME/.config/romarchy/backgrounds/$CURRENT_THEME_NAME"

mkdir -p "$THEME_USER_BACKGROUNDS"
xdg-open "$THEME_USER_BACKGROUNDS"