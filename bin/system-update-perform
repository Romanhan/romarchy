#!/bin/bash

set -e

hyprctl dispatch tagwindow +noidle &>/dev/null || true

update-keyring
update-system-pkgs
update-aur-pkgs
update-orphan-pkgs
update-restart

hyprctl dispatch tagwindow -- -noidle &>/dev/null || true

pkill -RTMIN+7 waybar 2>/dev/null || true